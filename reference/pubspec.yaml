# AUTO-GENERATED - Do not edit manually
# Generated by: dart run scripts/build_reference.dart
#
# This file aggregates all dependencies from brick templates.
# After running flutter pub upgrade, use sync_from_lock.dart
# to sync resolved versions back to bricks.

name: reference_deps
description: Reference project for brick dependency management
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  # Used by: arcane_beamer, arcane_app, arcane_dock
  arcane: ^6.5.3
  # Used by: arcane_models, arcane_server
  arcane_admin: ^1.5.8
  # Used by: arcane_beamer, arcane_app
  arcane_auth: ^1.8.1
  # Used by: arcane_beamer, arcane_app
  arcane_fluf: ^1.3.1
  # Used by: arcane_beamer, arcane_app
  arcane_user: ^1.1.0
  # Used by: arcane_beamer, arcane_cli, arcane_models, arcane_server, arcane_app
  artifact: ^1.0.22
  # Used by: arcane_beamer, arcane_app
  cached_network_image: ^3.4.1
  # Used by: arcane_beamer, arcane_app
  cloud_firestore: ^6.1.1
  # Used by: arcane_beamer, arcane_app
  convert: ^3.1.2
  # Used by: arcane_beamer, arcane_cli, arcane_models, arcane_app
  crypto: ^3.0.7
  # Used by: arcane_cli
  darted_cli: ^0.2.1
  # Used by: arcane_beamer, arcane_app
  decimal: ^3.2.4
  # Used by: arcane_beamer, arcane_app
  duration: ^4.0.3
  # Used by: arcane_beamer, arcane_app
  email_validator: ^3.0.0
  # Used by: arcane_server
  eventarc: ^1.0.3
  # Used by: arcane_beamer, arcane_app
  faker: ^2.2.0
  # Used by: arcane_beamer, arcane_cli, arcane_server, arcane_app, arcane_dock
  fast_log: ^1.0.7
  # Used by: arcane_beamer, arcane_app
  file_picker: ^10.3.7
  # Used by: arcane_server
  fire_api: ^1.5.2
  # Used by: arcane_server
  fire_api_dart: ^1.5.0
  # Used by: arcane_beamer, arcane_models, arcane_server, arcane_app
  fire_crud: ^3.1.14
  # Used by: arcane_beamer
  firebase_analytics: ^12.1.0
  # Used by: arcane_beamer, arcane_app
  firebase_auth: ^6.1.3
  # Used by: arcane_beamer, arcane_app
  firebase_core: ^4.3.0
  # Used by: arcane_beamer, arcane_app
  firebase_storage: ^13.0.5
  # Used by: arcane_dock
  flutter_acrylic: ^1.1.4
  # Used by: arcane_beamer, arcane_app
  flutter_native_splash: ^2.4.7
  # Used by: arcane_beamer, arcane_app
  flutter_svg: ^2.2.3
  # Used by: arcane_server
  google_cloud: ^0.2.0
  # Used by: arcane_beamer, arcane_app, arcane_dock
  hive: ^2.2.3
  # Used by: arcane_beamer, arcane_app, arcane_dock
  hive_flutter: ^1.1.0
  # Used by: arcane_beamer, arcane_cli, arcane_server, arcane_app
  http: ^1.6.0
  # Used by: arcane_beamer, arcane_app
  intl: ^0.20.2
  # Used by: arcane_beamer, arcane_app
  jiffy: ^6.4.4
  # Used by: arcane_dock
  launch_at_startup: ^0.5.1
  # Used by: arcane_server
  memcached: ^1.1.0
  # Used by: arcane_server
  mime: ^2.0.0
  # Used by: arcane_beamer, arcane_app, arcane_dock
  package_info_plus: ^9.0.0
  # Used by: arcane_cli
  path: ^1.9.1
  # Used by: arcane_dock
  path_provider: ^2.1.5
  # Used by: arcane_server
  precision_stopwatch: ^1.0.2
  # Used by: arcane_beamer, arcane_app, arcane_dock
  pylon: ^3.2.0
  # Used by: arcane_beamer, arcane_app
  rational: ^2.2.3
  # Used by: arcane_beamer, arcane_app
  rxdart: ^0.28.0
  # Used by: arcane_dock
  screen_retriever: ^0.2.0
  # Used by: arcane_beamer, arcane_app
  serviced: ^1.0.3
  # Used by: arcane_server
  shelf: ^1.4.2
  # Used by: arcane_server
  shelf_cors_headers: ^0.1.5
  # Used by: arcane_server
  shelf_router: ^1.1.4
  # Used by: arcane_beamer, arcane_app
  synchronized: ^3.4.0
  # Used by: arcane_beamer, arcane_app
  throttled: ^1.0.1
  # Used by: arcane_beamer, arcane_app
  timeago: ^3.7.1
  # Used by: arcane_beamer, arcane_app
  tinycolor2: ^3.0.1
  # Used by: arcane_beamer, arcane_cli, arcane_models, arcane_server, arcane_app, arcane_dock
  toxic: ^1.3.8
  # Used by: arcane_beamer, arcane_app, arcane_dock
  toxic_flutter: ^1.1.0
  # Used by: arcane_dock
  tray_manager: ^0.5.2
  # Used by: arcane_beamer, arcane_app
  tryhard: ^1.0.1
  # Used by: arcane_beamer, arcane_app
  universal_io: ^2.3.1
  # Used by: arcane_beamer, arcane_app, arcane_dock
  url_launcher: ^6.3.2
  # Used by: arcane_server
  uuid: ^4.5.2
  # Used by: arcane_dock
  window_manager: ^0.5.1
  # Used by: arcane_cli
  yaml: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  artifact_gen: ^1.1.6
  build_runner: ^2.10.4
  fire_crud_gen: ^1.1.4
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^6.0.0
  lints: ^6.0.0

flutter:
  uses-material-design: true
